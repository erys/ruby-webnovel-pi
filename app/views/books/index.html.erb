<div class="container px-2">
  <h1>Books</h1>

  <ul>
    <% @books.each do |book| %>
      <li>
        <a href="<%= book_path(book) %>">
          <%= book.tl_title %>
        </a>
      </li>
    <% end %>
  </ul>

  <%= link_to "Add Book", new_book_path %>
</div>
